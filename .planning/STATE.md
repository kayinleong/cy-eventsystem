# STATE — cy-eventsystem

**Project:** cy-eventsystem
**Owner:** kayinleong
**Current milestone:** v1
**Last updated:** 2026-05-25 (Phase 1 Plan 01-12 executed — 12/13 plans complete)

---

## Phase tracker

| Phase | ID | Status | Started | Completed |
|-------|----|----|---------|-----------|
| 1 | `phase-kayinleong-01` (UI POC) | In progress (12/13 plans complete) | 2026-05-24 | — |
| 2 | `phase-kayinleong-02` (Functionality) | Not started | — | — |

## Current focus

**Next step:** Execute plan 01-13 (`01-13-verification-gate-PLAN.md`) — Wave 4 verification gate. This is the Phase 1 acceptance-demo human-verify checkpoint that runs the full build/lint/typecheck/smoke validation across every route in the locked sitemap (now 100% surface-complete after Plan 12). Plan 13 likely doesn't add code — it produces a verification report + drives the human-verify checkpoint that closes Phase 1.

**Last session:** Phase 1 Plan 01-12 (Users + Settings) executed in 15 min, 2 atomic commits (10c4cb7 + e40f30c). Built the admin-only `/users` + `/users/invite` routes + the shared `/settings` route as composition over Plan 02's user/threshold mutators + Plan 03's DataTable substrate + Plan 04's role-gate helpers. **`components/feature/users/UsersTable.tsx`** — DataTable wrapper over `s.users` (raw slice + useMemo mutable spread per D-01-12-B) with 5 columns (Name + Email + Role + Created + actions); Disabled Badge inline on disabled rows (AUTH-09 surface); InviteUserSheet in toolbarExtras + empty-state action; UI-SPEC empty-state copy verbatim ("Just you, for now" / "Invite teammates to check items in and out."). **`components/feature/users/InviteUserSheet.tsx`** — admin-only Sheet (returns null for non-admin sessions) with rhf + zodResolver(InviteUserSchema) + Controller-bridged Select for role per D-01-06-B; dispatches store.inviteUser with resolved actor (D-01-05-E). **`components/feature/users/UserRoleSelectInline.tsx`** — inline 28x8 shadcn Select dispatching store.setUserRole (AUTH-08); short-circuits on no-change; disabled-passthrough. **`components/feature/users/DisableUserButton.tsx`** — destructive AlertDialog with UI-SPEC Q9 verbatim copy ("Disable this user?" / "They lose access immediately. Their past activity stays in reports." / "Disable user"); variant="destructive" on AlertDialogAction per D-01-06-F/D-01-04-F precedent; collapses (returns null) when alreadyDisabled. **`components/feature/settings/ThemePreferencesCard.tsx`** — next-themes Light/Dark/System radio with useSyncExternalStore-based `useHasMounted()` hook (D-01-12-A) replacing the lint-violating useEffect + setMounted pattern; SSR-safe (renders "system" placeholder on server). **`components/feature/settings/LowStockThresholdsCard.tsx`** — raw-slice + useMemo per D-01-11-A; ScrollArea-bounded h-80 list with per-item Input + Save button; admin-gated by `isAdmin` prop (disabled inputs + "Only admins can change thresholds." copy for staff) per D-01-12-D; dispatches store.updateLowStockThreshold. **Routes:** `app/(app)/users/page.tsx` + `app/(app)/users/invite/page.tsx` both admin-only via requireAdmin (CONTEXT.md D-07); `app/(app)/settings/page.tsx` accessible by all signed-in users via getMockSession + isAdmin prop derivation; plus the colocated client form at `app/(app)/users/invite/_components/invite-user-page-form.tsx` providing the dedicated full-page invite per UI-SPEC's "Layout & Route Patterns" full-page route entry for /users/new (D-01-12-C — Sheet + full-page parallel entries intentionally coexist; both share the rhf + zodResolver shape and dispatch the same store mutator). **3 deviations auto-fixed:** (1) **[Rule 3 - Blocking]** Built both invite forms (Sheet + full-page) on shadcn v4 `<Field>` primitives instead of the legacy `<Form>`/`<FormField>` Context wrapper the plan referenced — the v4 radix-nova registry ships the form entry as empty (D-01-04-B / D-01-06-A / D-01-07-A / D-01-11-B), so importing `@/components/ui/form` would have failed the build; (2) **[Rule 1 - Bug]** Fixed typescript readonly mismatch in UsersTable by reading raw `s.users` + useMemo spread to mutable array (D-01-12-B) — the store's readonly slice can't be passed directly to TanStack's mutable `data: T[]` contract; (3) **[Rule 1 - Bug]** Replaced useEffect+setMounted in ThemePreferencesCard with useSyncExternalStore-based useHasMounted hook (D-01-12-A) — same `react-hooks/set-state-in-effect` rule that D-01-02-A / D-01-03-A / D-01-10-C all consolidated against. Smoke test via curl against next dev: anon /users + /users/invite + /settings → 307 /login (app layout gate); admin /users + /users/invite + /settings → 200; staff /users + /users/invite → 307 /unauthorized (requireAdmin); staff /settings → 200 with "Only admins can change thresholds." gated copy. /users HTML contains all 5 seed users including Casey Ramirez with "Disabled" Badge; /users/invite HTML contains Email + Display name + Role + Send invite button + Back to users link; /settings HTML contains Theme card (Light/Dark/System) + Low-stock thresholds list with Save buttons. `npm run build` + `npx tsc --noEmit` + `npm run lint` all green (only known Plan-03 TanStack warning). The EVT-08 ↔ allowedStaff recompute happens automatically when a user is promoted via store.setUserRole — Plan 02's mutator computes the new admin union and overwrites every event's allowedStaff with the recomputed set, so inline role promotion from /users immediately changes which staff can see /events/[id]/checkout/checkin per EVT-08. Phase 1 surface is now 100% complete — all routes from the locked sitemap (verified by build's route table): /, /login, /register, /forgot-password, /set-password, /unauthorized, /inventory + /inventory/new + /inventory/[itemId] + /inventory/[itemId]/edit, /events + /events/new + /events/[eventId] + /events/[eventId]/edit + /events/[eventId]/checkout + /events/[eventId]/checkin, /scan, /reports/{stock,out,history,missing,repurchase}, /users + /users/invite, /settings. Plan 13's verification gate has a complete surface to validate. Resume file: `.planning/phases/phase-kayinleong-01/01-13-verification-gate-PLAN.md`.

**Prior session:** Phase 1 Plan 01-11 (Reports) executed in 11 min, 4 atomic commits (21e550e + e1a12ac + 8306b6d + 1ee2b59). Built the 5 read-mostly report routes (`/reports/{stock,out,history,missing,repurchase}`) as pure composition over Plan 02 selectors + Plan 03 DataTable substrate, plus the admin-only ResolveMissingSheet that dispatches store.resolveMissing. **`components/feature/reports/StockReportTable.tsx`** — REP-01 live stock with 9 columns (name + sku + category + availableQty + outQty + damagedQty + totalQty + threshold + status badge); name + availableQty sortable per D-11. **`components/feature/reports/ItemsOutTable.tsx`** — REP-02 wraps existing selectItemsOut projection. **`components/feature/reports/HistoryTable.tsx`** — REP-04 global tx log with 6 URL-synced filter keys (type/eventId/itemId/actorUid/from/to) + 3 toolbar Selects (in-component filtering per D-01-11-D); sortable by `at` server-timestamp; item + event cells link to detail pages. **`components/feature/reports/MissingItemsTable.tsx`** — REP-03/MIS-02 with 3 URL filter keys + default status=open per D-01-11-F; per-row Resolve action when row.status === 'open'. **`components/feature/reports/RepurchaseTable.tsx`** — REP-05/RP-04 dual-signal surface (low-stock primary + frequently-missing secondary) with admin-only inline Mark-as-ordered button on low-stock rows. **`components/feature/missing/ResolveMissingSheet.tsx`** — MIS-02/03/04 admin-only Sheet (per UI-SPEC Sheets vs Dialogs Shared #8) with rhf + zodResolver(ResolveMissingSchema); Controller-bridged RadioGroup for "Found" vs "Write off" (D-01-11-E); store.resolveMissing actor-resolution pattern from Plan 05. All 5 routes accessible by both admin AND staff per D-01-11-C — only mutator buttons (Mark-as-ordered + Resolve) are admin-gated via session.role check (REQUIREMENTS.md scopes admin-only behavior to AUTH-10's nav/buttons + RP-04's mark-ordered + MIS-03's resolve, not entire report routes). **2 deviations auto-fixed:** (1) **[Rule 3 - Blocking]** Built ResolveMissingSheet on shadcn v4 `<Field>` primitives instead of the legacy `<Form>`/`<FormField>` Context wrapper the plan referenced — the v4 radix-nova registry ships the form entry as empty (D-01-04-B / D-01-06-A / D-01-07-A), so importing `@/components/ui/form` would have failed the build; (2) **[Rule 1 - Bug]** Refactored StockReportTable's selector from inline `useMockStore((s) => s.items.filter(...))` to raw-slice + useMemo per D-01-11-A — the inline filter returned a new array reference on every snapshot read, triggering "Maximum update depth exceeded" + "getServerSnapshot should be cached" in dev mode. Pattern matches InventoryTable; pre-existing analogous warnings in EventsTable + RecentActivityFeed are out of scope but documented. Smoke test against next dev (cookie mock_session): anon → 307 /login on all 5 routes; admin → 200 on all 5 with PageHeader + disabled Export CSV + DataTable chrome rendered; staff → 200 on all 5; UI-SPEC empty-state copy verbatim ("Nothing missing" / "All checked-out items are accounted for."); zero infinite-loop errors after fix. `npm run build` + `npx tsc --noEmit` + `npm run lint` all green (only known Plan-03 TanStack warning). Resume file: `.planning/phases/phase-kayinleong-01/01-12-users-settings-PLAN.md`.

**Prior session:** Phase 1 Plan 01-10 (Check-in Flow) executed in 6 min, 2 atomic commits (ff825eb + 9d7af22). Built the per-event check-in reconciliation route at `/events/[eventId]/checkin` as a 4-file composition over Plans 02/03/09 substrate. **`components/feature/checkin/MissingReasonSelect.tsx`** — shadcn Select wrapping the 4 MissingReason enum values (Lost / Damaged / Not returned / Unknown per CI-04). `required` prop drives a destructive border + aria-invalid on the trigger so the CI-04 gate is visible inline. Maps empty-string prop to undefined Radix value so the placeholder shows correctly (D-01-10-F). **`components/feature/checkin/CheckinLineRow.tsx`** — per-line row with item link + sku + "Checked out: N" reference, plus three controls: Returned QtyStepper (max = checkedOut − damaged), Damaged QtyStepper (max = checkedOut − returned, CI-06 routes to item.damagedQty), Missing computed display (amber when > 0), Reason select (required when missingDelta > 0). Inline error row covers both CI-04 validation conditions (D-01-10-D + D-01-10-E). **`app/(app)/events/[eventId]/checkin/page.tsx`** — Server Component shell mirroring Plan 09's template: requireSession + async params + selectEventById + notFound + EVT-08 redirect. CRITICALLY does NOT reject any specific event status (unlike /checkout per D-01-10-A) — completed events may still have stragglers, cancelled events may have items to recover. Renders an EmptyState ("Nothing to check in") when selectOpenCheckoutsForEvent returns empty so the Client form never has to handle empty. **`app/(app)/events/[eventId]/checkin/_components/checkin-form.tsx`** — Client form with pure React state (NOT rhf per D-01-10-B because field count is driven by live store snapshot) and render-time two-track merge (`currentLines = lines ∩ liveOpen; missingFromState = liveOpen − lines; allLines = [...currentLines, ...missingFromState.map(buildLine)]` per D-01-10-C — avoids React 19's set-state-in-effect rule that Plan 02 D-01-02-A and Plan 03 D-01-03-A both hit). Default returnedQty = checkedOutQty per CI-03; submit validates every line (returned + damaged ≤ checkedOut AND missingDelta > 0 → missingReason set), assembles payload with parentTxId per line (CI-08), calls store.checkin atomically. Store.checkin creates MissingItemDoc records for delta lines (MIS-01) and routes damaged qty to item.damagedQty (CI-06) via Plan 02's existing per-item aggregation map. Success redirects to /events/[eventId]; remaining open lines stay visible on re-navigation (CI-07 partial check-ins). Verified via 6 smoke tests against next dev (cookie mock_session): anon → 307 /login; admin + active (evt-active-01) → 200 with 24 open-checkout rows + 48 QtyStepper decrease buttons + CI-03 defaults populated; admin + planned → 200 with "Nothing to check in" EmptyState; admin + completed (evt-completed-01) → 200 with 1 straggler row (confirming D-01-10-A); admin + missing event → 404; staff NOT in allowedStaff → 307 /unauthorized (EVT-08). Zero deviations — Plan 02's store.checkin signature aligned 1:1 with the form payload; Plan 09's server-shell template ported cleanly with only the status-policy distinction per D-01-10-A. `npm run build` + `npx tsc --noEmit` + `npm run lint` all green (only known Plan-03 TanStack warning). Resume file: `.planning/phases/phase-kayinleong-01/01-11-reports-PLAN.md`.

**Prior session:** Phase 1 Plan 01-09 (Checkout Flow) executed in 3 min, 1 atomic commit (f48fad0). Built the per-event check-out route at `/events/[eventId]/checkout` as a 2-file composition over Plan 08's substrate: **`page.tsx`** — Server Component shell mirroring `/events/[eventId]/page.tsx` and `/events/[eventId]/edit/page.tsx`: requireSession() + async params + selectEventById + notFound (missing event → 404) + EVT-08 access gate (admin OR uid ∈ event.allowedStaff; else 307 /unauthorized) + status-actionable reject (status !∈ {planned, active} → 307 /events/[eventId]) + generateMetadata for the SSR'd <title> ("Check out · Spring Product Demo"). **`_components/checkout-client.tsx`** — Client Component wrapping ScanSessionProvider (initialMode="checkout", initialEvent={event}) around PageHeader + Back-to-event link + CheckoutBody (ScanHeader + grid of [ScannerWidget + ManualEntryInput] + ScanCartPanel). The EventPickerDialog is bypassed because initialEvent is set at mount — confirmed by smoke test: rendered HTML has "Scanning for" sticky header but no "Pick event" CTA. Successful commit calls store.checkout atomically (CO-04); on success ScanSessionProvider.commit() already router.push('/events/[id]') back to event detail; on stock failure cart stays intact and failed lines surface as toast.error description (CO-05). Verified via 8 smoke tests against next dev (cookie mock_session): anon → 307 /login; admin + active/planned → 200; admin + completed/cancelled → 307 to event detail (status reject); admin + missing event → 404; staff NOT in allowedStaff → 307 /unauthorized; staff IN allowedStaff backup → 200. Rendered HTML for happy path contains all locked UI-SPEC elements. Zero deviations. Built the standalone scanner page at /scan and 6 reusable feature components in `components/feature/scan/`. **`scan-session.tsx`** — React context owning mode/selectedEvent/cart/isCommitting + addLine/removeLine/setQty/commit/endSession mutators (278 lines, comprises ~80% of plan business logic). The commit dispatches to `store.checkout` in checkout mode (atomic CO-04 commit, surfaces failed lines on CO-05 stock failure + leaves cart intact) or routes to `/events/[id]/checkin` in check-in mode (CI-02). Cart is ephemeral per page mount; RES-03 IndexedDB persistence deferred to Phase 2. **`ScannerWidget.tsx`** — `@yudiel/react-qr-scanner@2.6.0` with 5 formats (qr_code, code_128, ean_13, upc_a, data_matrix per CO-09 + D-16), rear-camera default (SCN-02), 1500ms duplicate-debounce via useRef (CO-07 per PITFALLS.md line 253), navigator.vibrate(50) haptic (CO-07), iOS-specific permission-error copy on `permission-denied` ScannerErrorKind OR `NotAllowedError` DOM name (SCN-03), tap-to-start camera lifecycle (battery hygiene per PITFALLS.md line 264), torch surfaced via `components={{ torch: true }}` (Chrome Android exposes it; iOS Safari simply doesn't render — verified IScannerComponents.d.ts has `torch?: boolean`). **`ScanCartPanel.tsx`** — QtyStepper + per-line remove + mode-aware CTA ("Check out N items" / "Return N items"), UI-SPEC "Cart is empty" empty-state copy verbatim. **`ScanHeader.tsx`** — D-15 sticky `top-14` header (matches TopBar offset) with selected-event name + status badge + End session button; returns null when no event selected. **`EventPickerDialog.tsx`** — shadcn Dialog + Command typeahead reusing `selectAccessibleEvents(s, uid, role, ["planned","active"])` for EVT-08 + CO-02. **`ManualEntryInput.tsx`** — SCN-06 typed-SKU input that submits on Enter; this Enter-key submit handler IS the CO-10 Bluetooth keystroke scanner handler (Bluetooth scanners emit keystroke bursts ending in Enter, indistinguishable from manual typing). **`app/(app)/scan/page.tsx`** — Client Component with Suspense wrapper around `useSearchParams` (Next 16 build-time requirement per node_modules/next/dist/docs/01-app/03-api-reference/04-functions/use-search-params.md lines 176-183); reads `?mode=checkin` for deep-link entry; composes Tabs mode toggle + ScanHeader + Pick-event empty state + ScannerWidget + ManualEntryInput + ScanCartPanel + EventPickerDialog under ScanSessionProvider. **3 deviations auto-fixed pre-commit:** (1) wrapped useSearchParams in inline Suspense to prevent Next 16 production build failure (the plan's example would have failed `npm run build`); (2) used `getSnapshot()` for one-off item lookup in addLine() instead of the closed-over `items` slice (lookup correctness across render frames per D-01-08-E) + removed an `as never` cast the plan's example used; (3) handled both library-normalized `IScannerError.kind` AND raw DOM `error.name` shapes in handleError so iOS-specific permission copy fires reliably across both error shapes (defense-in-depth, plan only checked the DOM name shape). Smoke test via curl against `next dev`: anon → 307 /login; admin /scan → 200 with "Scan items to check them out" + "Check out" tab + "Pick event" + "Cart is empty"; admin /scan?mode=checkin → 200 with "Scan items being returned" + "Return 0 items"; staff /scan → 200. `npm run build` + `npx tsc --noEmit` + `npm run lint` all green (only known Plan-03 TanStack warning). The scan substrate is now reusable: Plans 09 + 10 wrap `ScanSessionProvider initialEvent={event}` around the same ScannerWidget + ManualEntryInput + ScanCartPanel triplet at /events/[id]/checkout and /events/[id]/checkin. Resume file: `.planning/phases/phase-kayinleong-01/01-09-checkout-flow-PLAN.md`.

## Decisions (accumulated)

- **D-01-01-A:** Use shadcn v4 `<Field>` primitives for form composition. The legacy v3 `<Form>` / `<FormField>` Context wrapper has been removed from the radix-nova registry (entry exists but is empty). Plans 04, 06, 07, 12 must compose forms via `<Field>` / `<FieldLabel>` / `<FieldError>` and bind react-hook-form's `register` / `control` directly.
- **D-01-01-B:** Pin `react-day-picker` to v9. Shadcn's `calendar.tsx` references `classNames.table` which was removed in v10. Pin avoids paste-editing registry code per CLAUDE.md.
- **D-01-01-C:** Use Zod 4 canonical `z.email()` and `z.url()` top-level constructors instead of the deprecated `z.string().email()` / `z.string().url()` chains.
- **D-01-01-D:** `@hookform/resolvers` is v5.4.0 (not the v3 the plan listed). v5 is the only version that supports the React 19 + Zod 4 stack we shipped.
- **D-01-02-A:** `useCurrentUser` uses `useSyncExternalStore`, not `useEffect + useState`. React 19's `react-hooks/set-state-in-effect` ESLint rule flags synchronous `setState` inside `useEffect` as a cascading-render anti-pattern. The canonical React 19 pattern for syncing a non-React mutable source (`document.cookie`) into the component tree is `useSyncExternalStore` with a cached snapshot via JSON-key equality.
- **D-01-02-B:** All 14 store mutators inline their own `Object.freeze` rather than delegating through a shared wrapper. Makes the per-mutator immutability invariant trivially auditable (15 freeze sites: 14 mutators + 1 initial state).
- **D-01-02-C:** `checkout` aggregates cart lines by `itemId` before stock validation (Map-based). Without aggregation, a cart with two lines of the same item could pass per-line check yet violate the invariant on commit. `checkin` uses the same per-item aggregation pattern for outQty reduction.
- **D-01-02-D:** Mock `cookie.ts` uses dynamic `import("next/headers")` inside async server functions so a single module is importable from both server and client contexts (client consumers never invoke server functions, so the dynamic import never runs in browser bundles).
- **D-01-03-A:** `DataTableToolbar` re-syncs local input state from the parent's `globalFilter` prop using the React 19 canonical "previous value" render-time sync pattern, NOT `useEffect` + `setLocal`. Same anti-pattern family that Plan 02's `useCurrentUser` hit — React 19's `react-hooks/set-state-in-effect` rule flags synchronous setState inside effects. Store a `lastSyncedGlobal` sentinel in `useState`; if `globalFilter !== lastSyncedGlobal` during render, call `setLastSyncedGlobal(globalFilter)` + `setLocal(globalFilter)` directly in render. React's reconciler treats this as a render-time update and re-renders immediately without triggering the lint rule. Plans 06/07/10/11 should be aware when wiring any local table/form state from props.
- **D-01-03-B:** `useUrlTableState`'s `filterKeys` array is serialized to a `|`-joined string before the `useMemo` deps. If a consumer passes `filterKeys={['category','status']}` inline, the array identity changes every render and `useMemo` never memoizes. Strings are interned, so identical content always reaches the same dependency identity. Filter keys are static per call site by contract (Plans 06/07/10/11 each pass a fixed list).
- **D-01-03-C:** `DataTable` pagination chrome always renders (D-10) — even on empty data or filtered-to-zero. The empty state slot is rendered inside the table body via `<TableCell colSpan>` so the toolbar above and pagination below stay in their canonical positions. Two-tier empty precedence: source-empty (`data.length === 0`) renders the `emptyState` prop; filtered-empty renders "No results." inline.
- **D-01-03-D:** `DomainStatus` enum includes the missing-status value `'open'` → destructive tone. UI-SPEC marks `missing` as destructive; a `MissingItemDoc.status='open'` row is by definition the destructive case at the row level. Once resolved (`found` / `writtenOff`) the row collapses to muted tone.
- **D-01-03-E:** `DataTable` column visibility state lives in component state, NOT the URL. Toggling columns is an ephemeral user preference; URL-syncing it would pollute the back stack with every checkbox toggle. Pagination / sort / filters DO sync (REP-06's "shareable view" axes); column visibility is intentionally not.
- **D-01-04-A:** Delete `app/page.tsx` entirely; do NOT leave a stub redirect at root. Two pages at `/` would be a Next.js route-conflict error. The cleanest layout: `(app)/page.tsx` (Plan 05) owns `/`, and the role gate in `(app)/layout.tsx` handles unauth → `/login` via `requireSession()`. Verified via `curl` — anonymous request to `/unauthorized` returns 307 → /login.
- **D-01-04-B:** Form composition for all Phase 1 auth + entity forms uses shadcn v4 `<Field>` with rhf `register()` bound directly. Exact pattern: `<Field data-invalid={!!errors.X}><FieldLabel htmlFor="X">...</FieldLabel><Input id="X" {...register("X")} aria-invalid={!!errors.X}/><FieldError errors={errors.X ? [{message: errors.X.message}] : undefined}/></Field>`. `noValidate` on the `<form>` so browser native validation never competes with rhf+zod. Plans 06/07/08/12 must follow this exact shape.
- **D-01-04-C:** LoginForm rejects disabled users (AUTH-09) with the same error message as wrong password ("Wrong email or password.") per UI-SPEC error copy + T-04-04 mitigation. Separate "account disabled" copy would leak account-existence.
- **D-01-04-D:** PhaseOnePocRoleSwitcher uses `useCurrentUser()` (the useSyncExternalStore hook from Plan 02) for the radio value but reads the cookie fresh via `readMockSessionClient()` at flip time. Belt-and-braces — the cookie is source of truth, the hook is the render-time view. Short-circuits on `current.role === role` to avoid redundant writes.
- **D-01-04-E:** SeedUsersDisclosure uses `Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, 'value').set` + `dispatchEvent('input', {bubbles: true})` to set form values, NOT rhf Context. Decouples the disclosure file from the form file — Phase 2 deletes the disclosure wholesale without touching the form.
- **D-01-04-F:** SignOutButton uses `variant="destructive"` on `DropdownMenuItem` (a shadcn-supported prop) rather than ad-hoc className styling. Keeps destructive tone rule-driven per UI-SPEC Q9.
- **D-01-05-A:** Dashboard widget files follow the project convention from Plans 02 + 04 — comment header block ABOVE the `"use client"` directive (Next.js's directive scanner accepts comments before directives). The plan's literal acceptance criterion ("start with `\"use client\";`") is interpreted as "have the directive in the file header before any code", not "on line 1". Same convention used in lib/hooks/use-mock-store.ts, components/feature/shell/AppSidebar.tsx, and every (auth) form file.
- **D-01-05-B:** KpiCards `itemsOut` uses an inline `s.items.reduce((sum, i) => sum + i.outQty, 0)` instead of going through a dedicated selector. Plan 02's seed-data invariant guarantees `sum(item.outQty) == sum(open-checkout qty across active+overdue events)` so the inline reduce is equivalent to summing across `selectItemsOut()` but cheaper. If Phase 2 changes the invariant, add `selectTotalItemsOut(s)` to lib/mock/selectors.ts as a single-point swap.
- **D-01-05-C:** OverdueReturnsWidget rows link to `/events/[id]/checkin` (Plan 10), NOT `/events/[id]` (Plan 07's detail). Dashboard's job is recovery — sending the user to detail adds a click before the check-in action. Plan 07 will preserve cross-discoverability with a "Start check-in" CTA on the detail page.
- **D-01-05-D:** Dashboard route uses `getMockSession()` (not `requireSession()`) for the greeting. The `(app)/layout.tsx` role gate from Plan 04 already enforced auth, so session is guaranteed present at render time. Using `getMockSession()` directly avoids a redundant cookie read + redirect path. The greeting handles a defensive `null` case with `"there"` fallback.
- **D-01-05-E:** LowStockWidget's "Mark as ordered" button is admin-only at the UI level (AUTH-10 / RP-04). Widget reads `useCurrentUser()` and gates on `session?.role === "admin"`. Defense-in-depth — Phase 2's `markLowStockOrdered` Server Action will also enforce admin role server-side via `requireAdmin()` before mutating Firestore.
- **D-01-06-A:** ItemForm uses shadcn v4 `<Field>` primitives with rhf `register()` bound directly. Same convention Plan 04 established (D-01-04-B); the v3 `<Form>` / `<FormField>` Context wrapper is empty in the v4 radix-nova registry. Applies to every Plan 06+ form.
- **D-01-06-B:** ItemForm uses `<Controller>` for the category Select. shadcn Select exposes an imperative `onValueChange` (Radix pattern) that doesn't compose with `register()`. Controller bridges back to rhf's controlled-input model. Plans 07/11/12 will use the same pattern for their Select fields.
- **D-01-06-C:** After mutations call `router.push + router.refresh()` so the destination Server Component (which reads `getSnapshot()` server-side) re-evaluates and serves SSR'd HTML reflecting the new state. The store mutation is already visible to client components via `useSyncExternalStore` immediately; `router.refresh()` keeps the server tree in sync.
- **D-01-06-D:** InventoryTable's "Low stock only" toolbar filter predicate mirrors `selectLowStockItems` literally (lifecycleState != retired AND threshold > 0 AND availableQty <= threshold AND !lowStockOrderedAt). Guarantees the toolbar filter matches the dashboard's `LowStockWidget` rows exactly. Phase 2 swap: both move to a single Firestore query helper that returns the same projection.
- **D-01-06-E:** PrintLabelButton uses inline `<style>` for `@media print` CSS rather than a global stylesheet rule. Scope is limited (only active when dialog open + window.print() called); inline keeps the print behavior co-located with the component. Phase 2 may introduce a global print stylesheet if multiple components need print rules.
- **D-01-06-F:** RetireItemButton uses `variant="destructive"` on `AlertDialogAction` (shadcn-supported prop) rather than ad-hoc className. Keeps destructive-tone rule centralized to UI-SPEC Q9. Same approach as Plan 04's SignOutButton on DropdownMenuItem.
- **D-01-06-G:** ItemForm normalizes Zod-default fields (`unit`, `lowStockThreshold`, `notes`) via `values.field ?? default` at the submit boundary before calling the store mutator. Zod 4's `.default()` makes the rhf input type `T | undefined` but the mutator signature is strict. Normalizing at the boundary preserves the user-facing default while satisfying the typed contract.
- **D-01-07-A:** EventForm uses shadcn v4 `<Field>` primitives (continuing D-01-04-B / D-01-06-A convention). The plan's example imports from `@/components/ui/form` which doesn't exist (v4 radix-nova registry ships the `form` entry as empty). Bind rhf `register()` directly to shadcn primitives inside `<Field>`; use `<Controller>` for primitives that expose imperative APIs (Calendar `onSelect`, Combobox `onChange`).
- **D-01-07-B:** EventForm reads watched fields via rhf's `useWatch({ control, name })` instead of `useForm().watch()`. The ESLint rule `react-hooks/incompatible-library` flags `watch()` because its return value is a non-memoizable function — using it in render skips React Compiler optimization for the entire component. `useWatch` subscribes via FormProvider context and is memoization-safe. Plans 11/12 forms with reactive cross-field logic should use the same pattern.
- **D-01-07-C:** BackupTeamCombobox separates `allUsers` (for chip rendering — read once at top of component) from `pickerUsers = allUsers.filter(notIn(excludeUids))` (for the search list). The chip-rendering loop reads from `allUsers` so a previously-selected backup user who's now also a team lead doesn't vanish mid-edit (the `excludeUids` filters them out of the picker but the chip stays clickable to remove). Generalizes to any future combobox with cross-field exclusion.
- **D-01-07-D:** EventsTable takes `uid` + `role` props from its Server Component shell (the page reads `requireSession()` server-side and passes the session slice in), NOT via `useCurrentUser()` inside the table. Server-side `useCurrentUser` returns null (per D-01-02-A — no `document.cookie` on the server) which would yield an empty SSR list and an empty-state flash before client hydration fills in. Same pattern recommended for any future role-gated list (Plan 12 /users; possibly Plan 11 scoped reports).
- **D-01-07-E:** /events/new is admin-only via `requireAdmin()` (continuing CONTEXT.md D-07 strict gate). REQUIREMENTS.md EVT-01 allows team-lead creation; Phase 2 will relax the gate. Phase 1 keeps it strict so role-aware UI is testable end-to-end via the PhaseOnePocRoleSwitcher.
- **D-01-07-F:** /events/[eventId]/edit gating: `session.role === 'admin' || event.teamLeads.includes(session.uid)`. Staff in `allowedStaff` (e.g. backup-team members) can READ the detail page (EVT-08) but cannot edit (EVT-05) — they redirect to `/unauthorized`. Distinguishing read-access from edit-access keeps the data-layer simple (one `allowedStaff` field per event for reads) while EVT-05's tighter constraint lives at the page level.
- **D-01-07-G:** CancelEventDialog uses AlertDialog (not Sheet) for the EVT-06 reconciliation flow. UI-SPEC Q9 mandates AlertDialog for destructive confirmations. Long open-checkout lists are handled via a scrollable inner div (`max-h-72 overflow-y-auto`).
- **D-01-07-H:** EventDetail's Team card resolves displayNames via `useMockStore(s => s.users)` so renames + role changes propagate to chips live. Cost is one extra subscription per detail render — negligible given 5 seed users. Phase 2 swaps to a Firestore real-time listener on the `users` collection (or a per-event denormalized snapshot if read cost matters).
- **D-01-08-A:** `app/(app)/scan/page.tsx` wraps `useSearchParams` in `<Suspense fallback={null}>` inside the same file rather than splitting into a Server Component shell. Next 16 production builds fail with "Missing Suspense boundary with useSearchParams" without the wrapper (verified at node_modules/next/dist/docs/01-app/03-api-reference/04-functions/use-search-params.md lines 176-183). The plan's task-3 `<files>` block mandated a single file (`app/(app)/scan/page.tsx`), so co-locating Suspense in the same file preserves both the plan's file count and Next 16 build safety. Fallback is null because the (app) layout already renders sidebar+topbar chrome.
- **D-01-08-B:** ScanSessionProvider owns cart in `useState` (NOT `useOptimistic`). In Phase 1 the mock store commits synchronously — a separate optimistic shadow buys nothing. Documented for Phase 2 substitution: when checkout becomes a Server Action with async settle, swap `useState<ScanCartLine[]>` for `useOptimistic` and add a confirmed-state setter inside `commit()`.
- **D-01-08-C:** ScannerWidget uses tap-to-start instead of auto-activating the camera on mount. PITFALLS.md line 264 — auto-on drains battery if /scan is left open; iOS Safari permission prompts on auto-getUserMedia are jarring and frequently denied. Tap-to-start gives the user a clear gesture before the permission prompt fires.
- **D-01-08-D:** SCN-05 torch is exposed via `components={{ torch: true }}` on the Scanner, NOT a top-level prop. Verified via `node_modules/@yudiel/react-qr-scanner/dist/types/IScannerComponents.d.ts` — the `IScannerComponents` interface has `torch?: boolean`. Chrome Android renders the torch button automatically when the camera exposes `MediaStreamTrack.getCapabilities().torch`; iOS Safari doesn't expose torch and the button simply doesn't render. No platform branching needed.
- **D-01-08-E:** `scan-session.addLine()` reads the live snapshot via `getSnapshot()` for one-off item lookup INSTEAD of the closed-over `items` slice subscribed via useMockStore. Reason: the closed-over slice may lag by one render frame relative to the latest commit; `getSnapshot()` is synchronous and always returns latest state. The useMockStore subscription is still kept for setQty's max bound — a different concern (re-render trigger, not lookup correctness).
- **D-01-08-F:** EventPickerDialog defensively renders an empty list when `useCurrentUser()` returns null (the SSR + initial-paint case per D-01-02-A). The dialog only opens on user action ("Pick event" button) which is post-hydration — by mount, useCurrentUser has the correct session. Defensive empty-list rendering preserves SSR safety without an extra null-check spike.
- **D-01-08-G:** Cart commit in `"checkin"` mode does NOT call `store.checkin` directly — it routes to `/events/[eventId]/checkin` (CI-02). store.checkin needs parentTxId lookups + per-line returnedQty/damagedQty + missing-reason form fields that don't fit a single scan-then-confirm flow. The full check-in form lives in Plan 10; from /scan in checkin mode, the cart is a "select items to return then route to check-in screen" affordance.
- **D-01-08-H:** ScanHeader sticky offset is `top-14` (56px) to match the TopBar height from Plan 04. Verified by inspecting `app/(app)/layout.tsx` Plan 04 wiring. Phase 2 keeps this offset; if TopBar height changes, both surfaces must update together.
- **D-01-09-A:** `/events/[eventId]/checkout` uses a Server-shell + Client-island split rather than a single Client Component. `requireSession`, `notFound`, `redirect`, and async params are all server-only APIs — collapsing into a single Client file would force passing the event ID via search params + client-side fetching, losing SSR'd metadata title + adding a flash of empty content. The shell mirrors `/events/[eventId]/page.tsx` and `/events/[eventId]/edit/page.tsx` so the convention is consistent across the events subtree. Plan 10's `/events/[eventId]/checkin` will follow the same template.
- **D-01-09-B:** Status rejection on `/events/[eventId]/checkout` redirects to `/events/[eventId]` (the event detail page), NOT to `/events` list or `/unauthorized`. Cancelled and completed events are still readable (EVT-08 allows view-access); only the action surface is closed. The detail page already shows status-aware messaging per Plan 07 — landing the user there teaches them why the action is unavailable. Mirrors Plan 07's posture: once closed, the entity is read-only, not invisible.
- **D-01-09-C:** ManualEntryInput on `/events/[eventId]/checkout` renders without the `disabled` prop because the event is always pre-scoped from mount. On `/scan`, ManualEntryInput is disabled until `selectedEvent` is set; on the per-event route the event is permanent for the page lifetime so no guard is needed. Phase 2 may wire `disabled` to `scan-session.isCommitting` if a per-commit lock is added.
- **D-01-09-D:** No 'End session' suppression on `/events/[eventId]/checkout` — ScanHeader still renders the End session button for visual consistency with /scan. Clicking it clears cart + sets selectedEvent to null, which degrades the UI to "Pick an event below before scanning" without an EventPickerDialog mounted (the dialog is only in /scan, not the per-event route). Acceptable Phase 1 edge case; power-user behavior. Phase 2 may suppress End session on per-event routes or wire it to a Back-to-event redirect.
- **D-01-09-E:** `'use client'` directive in checkout-client.tsx is placed AFTER the comment header block, following the D-01-05-A convention established in Plan 05. Verified across every Plan 02/04/08 client file — Next.js's directive scanner accepts comments before directives.
- **D-01-10-A:** `/events/[eventId]/checkin` ACCEPTS any event status (unlike `/events/[eventId]/checkout` which redirects completed + cancelled per D-01-09-B). Stragglers and missing items may still be reconciled after an event closes — the seed evt-completed-01 has 1 open checkout (a missing mic from the original cycle) that's a valid check-in target. The status restriction lives ONLY on the checkout side because that's where new outbound activity gets gated. Phase 2's `checkinItem` Server Action preserves the same asymmetry: accept any status, validate the per-line invariants inside the Firestore transaction.
- **D-01-10-B:** Pure React state (NOT react-hook-form) for the check-in form because the field count is driven by the live store snapshot (CI-07 partial check-ins drop committed lines). rhf's `useFieldArray` would require either manual append/remove syncs OR a full form.reset() on every snapshot diff — both more fragile than direct React state. Phase 2 will wrap the same state shape in `useActionState` + a Server Action returning the same `CheckinResult` contract; the form's two-track merge stays unchanged.
- **D-01-10-C:** Render-time two-track merge (`lines ∩ liveOpen; liveOpen − lines`) instead of useEffect-driven state sync. Reason: same React 19 `react-hooks/set-state-in-effect` rule Plan 02 D-01-02-A hit with `useCurrentUser` and Plan 03 D-01-03-A hit with `DataTableToolbar`. Computes `currentLines` + `missingFromState` purely from props/state on every render; state only changes when the user edits a line OR submits. No effect needed; no lint warnings; deterministic behavior. Generalizes to any form whose shape is driven by a reactive data source.
- **D-01-10-D:** CI-04 validation duplicated in `CheckinLineRow` (inline destructive border + error message for feedback while editing) AND `CheckinForm.submit` (gating at commit time). Defense in depth — the row's inline error educates the user; the form's `validate()` re-runs at submit so a stale render or programmatic click can't sneak past. Both check the same two conditions: (a) `returnedQty + damagedQty ≤ checkedOutQty` and (b) `missingDelta > 0 → missingReason set`.
- **D-01-10-E:** Cross-bounded QtyStepper maxes prevent the user from pushing returned + damaged past checked-out via the +/- buttons: Returned's max = (checkedOutQty − damagedQty); Damaged's max = (checkedOutQty − returnedQty). Manual input falls through QtyStepper's internal clamp. Submit validates the same condition for defense in depth against programmatic state mutations.
- **D-01-10-F:** `MissingReasonSelect` coerces an empty-string prop to undefined on the Radix primitive (`value={value || undefined}`) so the placeholder shows correctly. Setting `value=""` on Radix Select produces a "controlled but empty" warning AND won't show the placeholder. Parent state continues to use `""` as the empty sentinel.
- **D-01-10-G:** Submit redirects to `/events/[eventId]` on success — same pattern Plan 09 uses for checkout. CI-07 partial check-ins are visible: re-navigating to /events/[eventId]/checkin will show remaining open lines. There's no per-line "skip" control — leave for later = cancel + come back. Phase 1 limitation per UI-SPEC; acceptable because the seed-data flow (1-3 open checkouts per event) makes "commit all now or come back later" the natural shape.
- **D-01-10-H:** `'use client'` directive placed AFTER the comment header block, following the D-01-05-A convention established in Plan 05.
- **D-01-11-A:** When a report table needs a filtered subset of a store slice, READ the raw slice via `useMockStore` (reference-stable across snapshots) and FILTER inside `useMemo`. The plan-suggested `useMockStore((s) => s.items.filter(...))` shape returns a new array reference on every snapshot read, breaking React 19's useSyncExternalStore identity check and triggering "Maximum update depth exceeded" + "getServerSnapshot should be cached" in dev. Pattern matches InventoryTable. EventsTable (`selectAccessibleEvents`) + RecentActivityFeed (`selectRecentActivity`) ship to prod with this anti-pattern and emit the same dev-only warning — out of scope for Plan 11 to refactor; documented for future cleanup.
- **D-01-11-B:** ResolveMissingSheet uses shadcn v4 `<Field>` primitives, NOT the legacy `<Form>` / `<FormField>` Context wrapper. The plan example referenced `@/components/ui/form` which does not exist in the v4 radix-nova registry (D-01-04-B / D-01-06-A / D-01-07-A all flagged the same import path as missing). Built using `<Field>` + `<FieldLabel>` + `<FieldError>` + rhf `register` / `Controller` exactly like ItemForm + EventForm.
- **D-01-11-C:** All 5 report routes accessible by both admin AND staff — only the inline mutator buttons (Mark-as-ordered in RepurchaseTable, Resolve in ResolveMissingSheet) are admin-gated via `session.role === 'admin'`. REQUIREMENTS.md scopes admin-only behavior to AUTH-10's nav/buttons + RP-04's mark-ordered + MIS-03's resolve; staff need to read /reports/missing to triage outstanding items for their events. Future Plan 12's `/users` route is the only Wave 3 admin-gated route in the (app) tree.
- **D-01-11-D:** HistoryTable filters in-component (not via a dedicated selector). The 6 filter keys (type/eventId/itemId/actorUid/from/to) are surface-specific; building a `selectFilteredTransactions(s, filters)` would duplicate the URL-decoder logic. Per-table predicate composes `url.filters` with raw `s.transactions`; selector layer stays general.
- **D-01-11-E:** ResolveMissingSheet's resolution radio choice is wrapped in `<Controller>` (NOT rhf `register`) because Radix RadioGroup exposes an imperative `onValueChange`. Same Controller bridge Plan 06 D-01-06-B established for shadcn Select.
- **D-01-11-F:** MissingItemsTable's status filter defaults to `open` (the actionable view). When the user selects "Open" from the dropdown, the URL drops the `status` param entirely — matches EventsTable's `status=active` default pattern (D-01-07-D family). The literal Radix value `_all` maps back to the unfiltered case.
- **D-01-12-A:** ThemePreferencesCard uses a useSyncExternalStore-based `useHasMounted()` hook (NOT useEffect+setMounted) for the next-themes SSR mount gate. The plan example used the canonical next-themes pattern `useEffect(() => setMounted(true), [])` which trips the project-wide `react-hooks/set-state-in-effect` ESLint rule (D-01-02-A useCurrentUser, D-01-03-A DataTableToolbar, D-01-10-C CheckinForm all consolidated on the same fix shape). The hook is a top-level const with module-level singleton callbacks: `emptySubscribe = () => () => {}`, `getClientSnapshot = () => true`, `getServerSnapshot = () => false`. Identical behavioral contract — `mounted` is `false` on SSR + first render, `true` after client hydration — but compatible with the lint rule. Reusable for any "need to know we're past initial paint" case.
- **D-01-12-B:** UsersTable bridges Plan 02's `readonly UserDoc[]` snapshot slice to TanStack DataTable's mutable `data: T[]` contract via raw-slice + useMemo shallow-spread copy: `const usersRaw = useMockStore((s) => s.users); const users = useMemo(() => [...usersRaw], [usersRaw]);`. The spread copies the reference array shape but preserves dependency identity via `useMemo([usersRaw])` so D-01-11-A's identity-stability rule is preserved while the typescript readonly mismatch is resolved. Same pattern InventoryTable + EventsTable use inside their filter useMemo bodies; Plan 12 is the first table that doesn't filter — so the spread is the entire useMemo body. Reusable for any future table that binds directly to a snapshot slice without intermediate filtering.
- **D-01-12-C:** Two parallel invite entries (Sheet on /users + full-page at /users/invite) intentionally coexist. UI-SPEC's "Sheets vs Dialogs" rule prefers Sheets for short forms (the 3-field invite form qualifies); AUTH-07 + UI-SPEC's "Layout & Route Patterns" require the dedicated /users/invite URL for deep-linking alongside /inventory/new + /events/new full-page routes. Both surfaces use the canonical shadcn v4 `<Field>` + rhf + zodResolver(InviteUserSchema) + Controller(Select) shape; both dispatch `store.inviteUser(values, actor)` with the D-01-05-E actor-resolution pattern. Only differences: Sheet closes + resets on success; full-page calls `router.push("/users") + router.refresh()` so the destination SSR re-evaluates with the new user.
- **D-01-12-D:** /settings is accessible by both admin AND staff at the route level (UI-SPEC sitemap puts Settings in the sidebar for both roles); the LowStockThresholdsCard's editor is admin-gated via an `isAdmin: boolean` prop passed from the Server Component shell — NOT via `useCurrentUser()` inside the card (that returns null on SSR per D-01-02-A and would flash "Only admins can change thresholds." for admins on first paint before snapping to the editable view). Personal theme preference (next-themes localStorage) is correctly accessible to all users; the admin-only constraint applies only to the threshold editor. Server-derived isAdmin yields correct SSR copy + correct gate state across both modes.
- **D-01-12-E:** UsersTable column sortability per D-11: only `displayName` is sortable. The Role column hosts the inline UserRoleSelectInline editor (filter-axis affordance, not a sort axis); the actions column hosts the per-row DisableUserButton. `email`, `role`, `createdAt`, `actions` carry the `// D-11: <col> is NOT sortable` audit comment. createdAt is intentionally non-sortable for Phase 1's 5 seed users (natural list order matches reality); Phase 2 may add sortability if user count justifies it.
- **D-01-12-F:** DisableUserButton uses `variant="destructive"` on AlertDialogAction (shadcn-supported prop per the Pick<ComponentProps, "variant" | "size"> shape on the v4 component) rather than ad-hoc className styling. Same approach D-01-06-F's RetireItemButton and D-01-04-F's SignOutButton established — keeps destructive tone rule-driven per UI-SPEC Q9 and consistent with prior destructive surfaces (Cancel event, Retire item, Write off, Resolve missing → Write off).

## Performance metrics

| Plan | Duration | Tasks | Files | Commits |
|------|----------|-------|-------|---------|
| 01-01 | 9 min | 2 | 41 | d8f9a6a, e5548bd |
| 01-02 | 18 min | 2 | 11 | feacb89, 7d45c17 |
| 01-03 | 7 min | 2 | 14 | 0ed298d, 491ec34 |
| 01-04 | 7 min | 2 | 19 | 4eac7cf, 2d00a01 |
| 01-05 | 5 min | 2 | 6  | 539dc09, 6813762 |
| 01-06 | 8 min | 3 | 11 | 203df5a, 2425d5f, 461ab7c |
| 01-07 | 9 min | 3 | 12 | 7fce96f, fed97f8, bed1059 |
| 01-08 | 7 min | 3 | 7  | 6370c32, 0133038, 267639b |
| 01-09 | 3 min | 1 | 2  | f48fad0 |
| 01-10 | 6 min | 2 | 4  | ff825eb, 9d7af22 |
| 01-11 | 11 min | 3 | 11 | 21e550e, e1a12ac, 8306b6d, 1ee2b59 |
| 01-12 | 15 min | 2 | 10 | 10c4cb7, e40f30c |

## Notes

- Repo was pre-initialized by user with `npx create-next-app` (Next 16.2.6) and `npx shadcn init` (v4 radix-nova/neutral) before GSD bootstrap.
- One shadcn component already installed: `components/ui/button.tsx`.
- `.env.local` does not exist and is not needed until Phase 2 Block A.
- No git history before this initialization commit.
- Per global CLAUDE.md, the owner-slug is `kayinleong` (derived from `ka.yin.leong`).
- All claim IDs and commit prefixes use the `phase-kayinleong-NN` / `quick-kayinleong-NNN` form.
- Phase 1 mock data layer (Plan 02) is Phase-2-swap-ready: Phase 2 swaps the body of `lib/mock/store.ts` (subscribe → onSnapshot, mutators → Server Actions) and the cookie decoder in `lib/mock/cookie.ts` — selectors + types + hook signatures stay verbatim.
- Phase 1 UI shell primitives (Plan 03) are Phase-2-swap-ready: every primitive in `components/ui/`, `components/feature/status/`, `components/feature/inventory/`, `components/feature/table/`, and the URL-state hooks in `lib/hooks/` are pure client-side and have no dependency on the mock store — Phase 2 reuses them verbatim with Firestore-backed selectors.
- Phase 1 auth spine (Plan 04) is now in place: `(auth)/layout.tsx` + 4 auth routes; `(app)/layout.tsx` role-gates via `requireSession()`; AppSidebar/TopBar/UserMenu/MobileNavSheet/Breadcrumbs compose the authenticated shell. Wave 3 plans (05-12) render their pages as `children` of `(app)/layout.tsx` — no per-page shell logic needed. Admin-only routes (Plans 06/07/12) call `requireAdmin()` from `lib/auth/mock-session.ts` (Plan 02) and bounce staff to `/unauthorized`.
- Phase 1 dashboard (Plan 05) is now in place at `app/(app)/page.tsx` with 5 client widgets in `components/feature/dashboard/`. Every widget reads via a named selector from `lib/mock/selectors.ts` (no inline filtering) and subscribes via `useSyncExternalStore` so any Wave 3 mutator's effect (checkout, checkin, resolveMissing, markLowStockOrdered) re-renders the dashboard automatically. The actor-resolution pattern for client-component-to-store-mutator calls is established here: `session = useCurrentUser(); actor = seedUsers.find(u => u.uid === session.uid); mutator(args, actor)`. Plans 06+ that expose inline store-mutator buttons should copy this shape.
- Phase 1 inventory (Plan 06) is now in place — 4 routes (`/inventory` list, `/inventory/new` admin create, `/inventory/[itemId]` detail, `/inventory/[itemId]/edit` admin edit) + 7 feature components in `components/feature/inventory/`. Patterns established here for Plans 07/11/12: (1) Server-Component shell + client-island-table composition via the generic DataTable + `useUrlTableState` for URL state; (2) admin-gated routes via `await requireAdmin()` at the top of the page; (3) shared form across create + edit with a discriminated-union `mode` prop; (4) shadcn v4 `<Field>` + rhf `register()` + `<Controller>` for Select fields; (5) Server-Component-detail + client-history-tab split (detail/page.tsx reads snapshot once for SSR; tab component subscribes via useMockStore for live updates); (6) destructive AlertDialog with UI-SPEC Q9 locked copy + `variant="destructive"` on AlertDialogAction; (7) bwip-js QR rendering via `bwip-js/browser` toCanvas; (8) print preview via inline `<style> @media print { ... }` scoped to `#print-label`. Last 3 patterns are specific to inventory but the first 6 generalize to every Wave 3 feature plan.
- Phase 1 events (Plan 07) is now in place — 4 routes (`/events` list, `/events/new` admin create per D-07, `/events/[eventId]` detail with EVT-08 access redirect, `/events/[eventId]/edit` admin+team-lead per EVT-05) + 8 feature components in `components/feature/events/`. New patterns established here for Plans 08-12: (1) **SSR-truthful role projection** — list pages read `requireSession()` server-side and pass `session.uid` + `session.role` as props to client tables, NOT via `useCurrentUser()` which returns null on SSR; (2) **Multi-select combobox** — shadcn Command + Popover + cmdk + Badge chip rendering, with `excludeUids` prop for cross-field exclusion (BackupTeamCombobox excludes selected team leads); (3) **Date input pattern** — shadcn Calendar in Popover + rhf Controller bridging `onSelect`/`selected` to ISO string state; (4) **rhf reactive reads** — use `useWatch({control, name})` instead of `useForm().watch()` to avoid the React Compiler `react-hooks/incompatible-library` warning; (5) **Status-aware primary CTA** — detail-page hero renders a dynamic primary action based on entity status (planned → Start check-out, active → Check in, terminal → none); (6) **EVT-06 reconciliation dialog** — destructive AlertDialog enumerating open transactions with per-row Select for resolution choice, single Confirm passes full reconciliation array to store.cancelEvent. Patterns 1-4 generalize broadly; 5-6 are specific to status-bearing entities but the reconciliation shape applies to any cancel-with-cleanup flow.
- Phase 1 scanner substrate (Plan 08) is now in place — 1 route (`/scan` with `?mode=` deep-link) + 6 feature components in `components/feature/scan/` + a `ScanSessionProvider` React context. `ScannerWidget` + `ScanCartPanel` are deliberately reusable: Plans 09 (`/events/[id]/checkout`) and 10 (`/events/[id]/checkin`) wrap the same `ScanSessionProvider` with `initialEvent={event}` (passed in from the Server Component shell) so the user skips the EventPickerDialog. Patterns established here for Plans 09 + 10: (1) **Scan-session React context** — feature-scoped context owning ephemeral state (mode, sticky event, cart) with useCallback'd mutators and a `useScanSession()` hook that throws outside the provider; (2) **Wrapping useSearchParams in inline Suspense** — Next 16 production-build requirement (verified in node_modules docs); fallback `null` because the (app) layout already renders the chrome; (3) **Tap-to-start camera lifecycle** — stateful `active: boolean` gates `<Scanner/>` mount; Stop button + unmount effect release MediaStream for battery hygiene; (4) **Same-handler dispatch from 3 input paths** — camera (debounced + vibrate), manual entry (Enter or Add button), Bluetooth keystroke scanner (Enter at end of burst) all land in `addLine(value)` from the scan-session context; no platform branching; (5) **Library error normalization** — checks both `IScannerError.kind` (library) AND raw DOM `error.name` (NotAllowedError, NotFoundError) so iOS-specific UI-SPEC copy fires reliably; (6) **getSnapshot() for one-off lookup** — addLine reads via the synchronous store API, not a closed-over slice, so lookups stay correct across render frames. The Phase 2 swap is minimal: `store.checkout` becomes a Server Action returning the same `CheckoutResult` contract; the only piece that changes is the cart's `useState` → `useOptimistic` swap inside `commit()`.
- Phase 1 per-event checkout (Plan 09) is now in place — 1 route (`/events/[eventId]/checkout`) built as 2 files: a Server Component shell (page.tsx) + colocated Client island (_components/checkout-client.tsx). Pure composition over Plan 08's substrate — zero new feature components. **The Server-shell + Client-island template is now the canonical shape for /events/[eventId]/* action routes**: the shell does `requireSession() + await params + selectEventById + notFound + EVT-08 (admin OR uid ∈ allowedStaff; else /unauthorized) + status-actionable reject (status !∈ {planned, active} → redirect to event detail) + generateMetadata`; the Client island wraps `<ScanSessionProvider initialMode='...' initialEvent={event}>` around the reused Plan 08 components (ScanHeader + ScannerWidget + ManualEntryInput + ScanCartPanel). Plan 10's `/events/[eventId]/checkin` will copy-paste-and-rename this shell with `initialMode="checkin"` plus its own status check (`status === "active"` only — planned events have no open checkouts yet, completed/cancelled are closed). The substrate's commit() body handles success redirect to `/events/[eventId]` (via the existing `router.push(`/events/${selectedEvent.id}`)` in scan-session.commit() for checkout mode) and CO-05 stock failure (toast.error with failedLines description, cart stays intact) — no extra logic at the page level. Phase 2 swap surface: page.tsx's requireSession swaps to a DAL-wrapped verifySession; selectEventById swaps to a Firestore getDoc inside the DAL; checkout-client.tsx doesn't change at all (Phase 2's store.checkout swap is inside scan-session.commit, invisible to this route).
- Phase 1 per-event check-in (Plan 10) is now in place — 1 route (`/events/[eventId]/checkin`) built as 4 files: server shell (`page.tsx`) + client form (`_components/checkin-form.tsx`) + 2 reusable feature components in `components/feature/checkin/` (MissingReasonSelect, CheckinLineRow). Server shell mirrors Plan 09's template (requireSession + async params + selectEventById + notFound + EVT-08 redirect) BUT does NOT reject any event status per D-01-10-A (completed/cancelled events may still have stragglers — confirmed by the evt-completed-01 seed's 1 open straggler line). EmptyState ("Nothing to check in") renders when selectOpenCheckoutsForEvent returns empty so the client form never handles empty. Client form is pure React state (NOT rhf per D-01-10-B because field count is driven by live store snapshot for CI-07 partial check-ins) with a render-time two-track merge (`currentLines = lines ∩ liveOpen; missingFromState = liveOpen − lines` per D-01-10-C — avoids React 19's set-state-in-effect rule that Plan 02 D-01-02-A and Plan 03 D-01-03-A both hit). Default returnedQty = checkedOutQty per CI-03; submit validates per-line (returned + damaged ≤ checkedOut AND missingDelta > 0 → missingReason set per CI-04 — validation duplicated inline in CheckinLineRow + in submit per D-01-10-D for defense in depth), assembles payload with parentTxId per line (CI-08), calls store.checkin atomically. The mutator routes damaged qty to item.damagedQty (CI-06) and creates MissingItemDoc records for delta lines (MIS-01) via Plan 02's existing per-item aggregation map + missing-record creation logic. Cross-bounded QtyStepper maxes (D-01-10-E) prevent + manual input from exceeding checkedOutQty; MissingReasonSelect maps empty-string to undefined Radix value so placeholder renders (D-01-10-F). 6 smoke tests pass: anon → /login; admin + active → 200 with 24 rows + CI-03 defaults populated; admin + planned → 200 with EmptyState; admin + completed → 200 with 1 straggler row (D-01-10-A); admin + missing event → 404; staff NOT in allowedStaff → /unauthorized. Zero deviations — Plan 02 store.checkin signature aligned 1:1 with form payload; Plan 09's server-shell template ported cleanly with only the status-policy distinction. Phase 2 swap surface: requireSession's body changes to DAL-wrapped verifySession; selectEventById + selectOpenCheckoutsForEvent become Firestore reads inside the DAL; checkin call becomes `await checkinItem(...)` Server Action with same CheckinResult contract; useMockStore subscription swaps to Firestore onSnapshot — the two-track merge stays unchanged because it operates purely on the typed snapshot.
- Phase 1 reports (Plan 11) is now in place — 5 routes (`/reports/{stock,out,history,missing,repurchase}`) + 5 tables in `components/feature/reports/` + 1 admin-only Sheet in `components/feature/missing/` (11 files total). Pure composition over Plan 02's selectors/mutators + Plan 03's DataTable substrate; zero new selectors added to `lib/mock/selectors.ts`. The only new mutator-consumer is RepurchaseTable's inline Mark-as-ordered button (wraps store.markLowStockOrdered with the Plan 05 actor-resolution pattern) and ResolveMissingSheet's submit handler (wraps store.resolveMissing). All 5 routes accessible by both admin AND staff per D-01-11-C — only mutator buttons admin-gated via session.role === 'admin'. Smoke test confirms: anon → 307 /login (the (app) role gate from Plan 04); admin → 200 on all 5; staff → 200 on all 5. UI-SPEC empty-state copy verbatim ("Nothing missing" / "All checked-out items are accounted for." per REP-03; "Nothing checked out" / "No items are currently at events." per REP-02). HistoryTable's 6-key URL-synced filter (type/eventId/itemId/actorUid/from/to) is the most complex of any Wave 3 table; the in-component predicate keeps the selector layer general per D-01-11-D. ResolveMissingSheet uses the canonical Phase 1 form shape (shadcn v4 `<Field>` + rhf + zodResolver) with Controller-bridged RadioGroup per D-01-11-E — the legacy `<Form>` Context wrapper the plan example referenced does not exist in v4 (D-01-04-B / D-01-06-A / D-01-07-A confirmed). 2 deviations auto-fixed: Rule 3 (the missing `@/components/ui/form` import) caught pre-write; Rule 1 (StockReportTable's inline filter causing "Maximum update depth exceeded") caught via dev-mode smoke test post-commit and fixed via D-01-11-A (cache the raw slice; filter in useMemo). Phase 2 swap surface: every table's `useMockStore` subscription swaps to a Firestore onSnapshot wrapper returning the same typed shape; the URL state hooks + DataTable wrapper + filter predicates stay verbatim; the Sheet's `resolveMissing` call becomes `await resolveMissingItem(...)` Server Action with same signature.
- Phase 1 users + settings (Plan 12) is now in place — 3 routes (`/users` admin-only list, `/users/invite` admin-only full-page form, `/settings` shared admin+staff) + 4 user-feature components in `components/feature/users/` + 2 settings cards in `components/feature/settings/` + 1 colocated client form (10 files total). Pure composition over Plan 02's user/threshold mutators + Plan 03's DataTable substrate + Plan 04's requireAdmin/requireSession helpers. New patterns established (carry into Phase 2's Block B users wiring): (1) **useSyncExternalStore-based useHasMounted hook** (D-01-12-A) — replaces the lint-violating useEffect+setMounted pattern for SSR-safe one-shot mount detection (consolidates D-01-02-A/D-01-03-A/D-01-10-C against the project-wide `react-hooks/set-state-in-effect` rule); reusable for any next-themes / localStorage / window-size case. (2) **Mutable-spread bridge for DataTable** (D-01-12-B) — read raw readonly snapshot slice via useMockStore + useMemo shallow-spread to a mutable T[] copy; satisfies TanStack's `data: T[]` contract while preserving Plan 02's `Object.freeze` immutability invariant and D-01-11-A's identity-stability rule. Reusable for any future table that binds directly to a snapshot slice without intermediate filtering. (3) **Sheet + full-page parallel invite entries** (D-01-12-C) — UI-SPEC's "Sheets vs Dialogs" rule (Sheet for short forms) coexists with AUTH-07's deep-linkable /users/invite URL; both surfaces share the rhf + zodResolver(InviteUserSchema) + actor-resolution dispatch shape, only the chrome differs (Sheet closes + resets; full-page router.push + router.refresh so destination SSR re-evaluates). (4) **Server-derived isAdmin prop for mixed-role routes** (D-01-12-D) — /settings is accessible by all signed-in users but the threshold editor is admin-only; Server Component resolves `isAdmin = session?.role === "admin"` via getMockSession and passes the boolean down to LowStockThresholdsCard, avoiding the useCurrentUser-returns-null-on-SSR flash that D-01-02-A documented. EVT-08 ↔ allowedStaff recompute is automatic: store.setUserRole (Plan 02 mutator) computes the new admin union and overwrites every event's `allowedStaff`, so inline role promotion from /users immediately changes which staff can see /events/[id]/checkout/checkin per EVT-08. 3 deviations auto-fixed all pre-commit: Rule 3 (the missing `@/components/ui/form` import on both InviteUserSheet + InviteUserPageForm — built on canonical shadcn v4 `<Field>` instead per D-01-04-B/D-01-06-A/D-01-07-A/D-01-11-B); Rule 1 (UsersTable's `readonly UserDoc[]` to DataTable's mutable `T[]` mismatch via D-01-12-B); Rule 1 (ThemePreferencesCard's useEffect+setMounted lint violation via D-01-12-A). Phase 2 swap surface: every component's store-mutator call becomes a Server Action with the same signature (`inviteUser`, `setUserRole`, `disableUser`, `updateLowStockThreshold` → `inviteUserAction`, `setUserRoleAction`, etc.); the actor argument becomes `verifySession()` server-side; InviteUserSheet's submit will swap to async with Firebase password-reset link delivery per PROJECT.md decision #2; the useHasMounted + mutable-spread + isAdmin-prop patterns stay verbatim. **Phase 1 surface is now 100% complete** — `npm run build`'s route table confirms all 22 routes from the locked sitemap (/, /login, /register, /forgot-password, /set-password, /unauthorized, /inventory + /inventory/new + /inventory/[itemId] + /inventory/[itemId]/edit, /events + /events/new + /events/[eventId] + /events/[eventId]/edit + /events/[eventId]/checkout + /events/[eventId]/checkin, /scan, /reports/{stock,out,history,missing,repurchase}, /users + /users/invite, /settings). Plan 13's verification gate has a complete surface to validate.

## Open clarifications (carried into Phase 2 planning)

These do not block Phase 1 but should be answered before Phase 2 work begins in earnest:

1. Existing barcodes the customer needs to scan vs all-new labels?
2. Expected inventory volume? (Affects index strategy + listener cost.)
3. Email delivery: Firebase built-in for invites + low-stock — sufficient, or need SendGrid?
4. Photo storage scope: item photos? Damage attachments? Affects Storage rules + CDN.
5. `next-firebase-auth-edge` v1.12 stability — validate with a 1-day spike at start of Phase 2.
